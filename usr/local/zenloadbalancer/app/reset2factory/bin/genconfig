#!/bin/bash
confdir="/usr/local/zenloadbalancer/config"
pwfile="/usr/local/zenloadbalancer/app/mini_httpd/.htpasswd"
host="/etc/hostname"

files="$confdir $pwfile $host"
cd /
tar -czvf /usr/local/zenloadbalancer/app/reset2factory/config/zlbdefault.tgz $files

exit 0
